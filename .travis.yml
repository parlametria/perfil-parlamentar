language: node_js
node_js:
  - "10"
dist: trusty
sudo: required
branches:
  only:
  - refatoramento-frontend
before_script:
  - cd client/
  - npm install -g @angular/cli
  - npm install
script:
  - ng lint
  - ng test --watch=false --browsers=ChromeHeadless
  - ng build --prod